package com.chris.studyplanner.ui.theme

import androidx.compose.ui.graphics.Color

// --- Paste everything below this line into your EXISTING Color.kt, ---
// --- underneath your Purple80/Purple40/etc. declarations.           ---

// Backgrounds
val DarkBackground = Color(0xFF1B1512)      // main screen background
val DarkSurface = Color(0xFF241C18)         // input fields, base surface
val DarkSurfaceVariant = Color(0xFF2E2420)  // nested cards, chips background

// Accent (coral)
val CoralAccent = Color(0xFFE8845B)         // buttons, borders, highlighted text
val CoralAccentDim = Color(0xFFB86645)      // pressed/disabled state of accent

// Text
val TextPrimary = Color(0xFFF5EDE8)         // titles, primary text on dark bg
val TextSecondary = Color(0xFFB8A99E)       // subtitles, muted text
val TextOnAccent = Color(0xFF1B1512)        // text drawn on top of coral (e.g. button label)

// Borders / outlines
val OutlineCoral = Color(0xFFD2734C)        // card border color (result card outline)
val OutlineSubtle = Color(0xFF3A2E28)       // input field default border